# W3-003-RM3-V1 — Minimal P0 Release Remediation Implementation

## Decision

Status: `READY_FOR_SENIOR_REVIEW`.

The single authorized candidate-validation campaign passed all preregistered
development thresholds: STANDARD 12/12, SAFE_CORRECTIVE 10/10, and HARD 8/8.
All zero-tolerance safety counters are zero and the largest unresolved mechanism
count is zero. This is development evidence only; it is not an EV2 rerun, an EV3
authorization, or a product-gate claim.

## Authority and preflight

- Task: `W3-003-RM3-V1`, Week 3 P0 remediation.
- Branch: `main`.
- HEAD and `origin/main`: `7160590ae0b236171894ebce297662b7063e0897`.
- Tree: `1870dba65ca5972d000d64238830f2a138df8a4c`.
- Staged paths: 0.
- A1 readiness, remediation contract, and causal-summary hashes matched exactly.
- Existing unrelated dirty/untracked work was preserved.

## Fixture freeze and independence

Before production edits, 30 invariant-derived development fixtures were frozen
at SHA-256 `b56e1ef86c1e379bec97f31b7b02c5010679ec898c078cafaddadf49a5133afc`:
12 STANDARD, 10 SAFE_CORRECTIVE, and 8 HARD_ABSTAIN. The plan covers all 16
required behavioral families.

The read-only leakage guard inspected the locked EV1/EV2 text only to detect
leakage. It found zero exact normalized text duplicates, zero case-ID references,
zero expected-evidence-ID references, and zero shared normalized 12-word windows.
No locked text was used to design or tune a fixture.

## Implementation

### P0-A — obligation-complete STANDARD selection

- `support_v2.detect_requested_dimensions` returns every explicit requested
  dimension in stable rule order; the existing singular API remains compatible.
- Sentence matching now recognizes all explicit dimensions rather than only the
  first match.
- `routing_v3.assess_requested_target` fails closed for unknown or conflicting
  transaction states and checks complete support for every requested dimension.
- `select_supported_standard_objectives` requires an exact support assignment for
  every dimension, prevents reuse of one evidence sentence for two different
  obligations, preserves state/domain binding, and enforces timing authority.
- Partial obligation cover yields ABSTAIN, never a partial STANDARD answer.

### P0-B — route and factual authorization

- Existing guarantee/exact/private guards remain in force.
- General account-specific outcome and prohibited process-action slots were added
  to the existing specificity contract; these cannot authorize STANDARD.
- Unknown/conflicting state, wrong state/domain, account-specific outcome,
  guarantee, private/control-plane, partial cover, and citation/ineligibility
  boundaries all fail closed.
- No new external response taxonomy was introduced.

### P0-C — bounded corrective discovery

- The production config now carries the explicit `corrective_discovery` policy:
  enabled only for SAFE_CORRECTIVE, approved/effective evidence only, no transition
  to STANDARD, no override of failed STANDARD authorization, no unbound factual
  resolution, and ABSTAIN fallback.
- Existing caps remain 8 candidate chunks and 128 scanned chunks.
- Corrective discovery is reachable only after an independently blocked route and
  an exact business scope/state anchor.
- Wrong-state or missing anchors do not trigger alternate recovery. Incomplete
  corrective obligation cover, DRAFT evidence, and expired evidence abstain.
- Setting `corrective_discovery.enabled=false` is a tested rollback off-switch that
  forces ABSTAIN without changing the other fail-closed policy fields.

## Changed production/config paths

- `src/payresolve_ai/generation/support_v2.py`
- `src/payresolve_ai/generation/routing_v3.py`
- `src/payresolve_ai/generation/pipeline_v3.py`
- `configs/generation/grounded_pipeline_v3.json`

Conditional paths `gate.py` and `corrective_v1.py` were not changed. The read-only
retrieval runtime remained byte-identical at SHA-256
`8e09bcb978d5fe921fb978246f61f1711560830092ea7fdeb0d62e39d321fe27`.

## Verification

- Focused RM3/RM2 run: 25 tests passed plus 35 subtests before the final off-switch
  case was added.
- Grounded/safety/RED1/Option-A run: 40/40 passed.
- Complete focused pre-freeze run: exit 0 across 199 statically counted test
  methods plus parameterized/subtest instances.
- Python compilation: PASS.
- `git diff --check`: PASS.
- Selected A3 integrity checks: 5 passed; one historical preflight lock failed
  because it deliberately binds the pre-RM3 production/config bytes. Its receipt
  showed only the four authorized RM3 production/config paths as drift; frozen
  A2, EV1, critical-rev7, manifest, and R0 identities remained exact. The old test
  was preserved unchanged.

## Single candidate validation

Exactly one campaign ran after `V1_CANDIDATE_COMPLETE` and candidate hash freeze.
No source/config/test/fixture membership changed afterward.

| Stratum | Success | Required | Result |
|---|---:|---:|---|
| STANDARD | 12/12 (1.00) | denominator ≥12, rate ≥0.80 | PASS |
| SAFE_CORRECTIVE | 10/10 (1.00) | denominator ≥10, rate ≥0.75 | PASS |
| HARD | 8/8 (1.00) | denominator ≥8, rate =1.00 | PASS |

Unauthorized STANDARD routes, invalid citations, ineligible evidence use, and
payload-bearing hard abstentions were all zero. There were no failed fixtures and
no unresolved mechanism.

The frozen official R1 aggregates were 10/24 STANDARD, 1/18 SAFE_CORRECTIVE,
6/12 HARD, and 41 failed cases. The new 30-case results are intentionally not
presented as a like-for-like improvement: they are independent invariant-derived
development fixtures that demonstrate the targeted mechanisms, not EV2 cases.

## Protected boundaries and mutations

- EV2 candidate/evaluator/scorer calls: 0.
- Gold loads by RM3 validation: 0; Gold changes: none.
- Semantic retriever/model/network calls: 0.
- KB, evaluator, retrieval runtime, EV2 raw artifacts, and frozen evidence: unchanged.
- Stage/commit/push: none.
- EV3 and FIX1: not started.
- Canonical `PROJECT_STATE.md`, `TASKS.md`, daily, and weekly publication were not
  updated because this contract ends at local Senior-review evidence and grants no
  lifecycle publication authority.

## Requested Senior decision

Please review the detached bundle and decide whether to approve RM3-V1 for the
next explicitly authorized publication or independent-gate step. No further
execution is implied by this result.
