# W3-003-EV2-A1 — EV2 preregistration and development-only mutation precheck

## Status

`IMPLEMENTED_DEVELOPMENT_PRECHECK_PASS_AWAITING_INDEPENDENT_SENIOR_REVIEW`.

This report is **DEVELOPMENT REGRESSION EVIDENCE ONLY — NOT PRODUCT APPROVAL**.
It does not authorize EV2, A2, Week 3 completion, or Week 4.

## R2 post-TB1 candidate rebind and mandatory precheck — 2026-08-21

Senior approved TB1 solely to resume A1. Before execution, the fresh remote,
TB1 routing/test bytes, frozen production/runtime identities, contract inputs,
and evaluator/fixture hashes matched the reviewed bundle. FIX1 PRIMARY, summary,
and manifest were copied to `*_fix1_pre_tb1` historical paths byte-for-byte
before the active filenames were replaced.

The post-TB1 candidate is accurately recorded as
`SENIOR_APPROVED_TB1_WORKING_TREE_CANDIDATE`: it is byte-frozen for this A1
recheck, `candidate_committed=false`, and `candidate_pushed=false`. PRIMARY ran
once and passed 15/15 with every hard counter at zero. The one authorized
REPRODUCTION also passed 15/15; fixture order, routes/reasons, selected evidence,
claims/citation semantics, verifier verdicts, and counters are deterministic.

This is **DEVELOPMENT REGRESSION EVIDENCE ONLY — NOT PRODUCT APPROVAL**. EV2 is
not authored, authorized, executed, or consumed; A2 is not started; W3 P0 and
Week 4 remain blocked pending final independent Senior review.

## TB1 bounded same-domain target-binding repair — 2026-08-21

The confirmed `EV2DEV-04` defect was localized to
`_single_checks_target()` selecting the requested `account` target and
`_sentence_matches_single_checks_target()` accepting any candidate sentence
that contained an account-equivalent token. The failing sentence's substantive
check object was mobile-device registration, but its incidental `account` token
was sufficient under that predicate.

TB1 adds a deliberately narrow conflict predicate for account-target CHECKS:
mobile-device registration/profile/settings and customer-profile settings no
longer establish account-target support merely because `account` is also
present. The repair does not add a broad target ontology and preserves direct
recipient-account detail/information checks plus target-unspecified generic
checks.

Test-first evidence: the two new TB1 test methods failed before the production
change with four assertion failures (the exact regression plus three
high-overlap negative variants), then passed 2/2 after the change. The focused
remediation suite passed 22/22; safe V3 allowlist suites passed 24/24; the RED1
verification boundary suite passed 5/5; and the non-consumed synthetic W3-003
development replay passed 14/14 with normalized SHA-256
`285bcc3187eeb7252cbe9f4c9d61fca00fc57af8cba873ae83e4b2df72ca4a6a`.

The A1 PRIMARY evidence remains the immutable pre-repair result: 14/15, with
`EV2DEV-04` failing. TB1 did not run A1 precheck/reproduction, author A2/EV2,
or access consumed evidence. Therefore TB1 is development regression evidence
only and awaits independent Senior review before any separately authorized A1
recheck.

## Bound candidate and boundary

The precheck bound RM2 publication commit
`cd97de602140e334ec499e8dfa27fa08ec1a6260`, with lifecycle closure
`89b62545915f6e2a9ac63f64f7a8fccc47145388`. Five published production source
identities plus the production pipeline config, lexicon, and frozen KB input
matched the A1 contract. The fixture set contains exactly 15 fresh development
scenario families and each row is `DEVELOPMENT_ONLY`, EV2-excluded, and marked
non-consumed.

No EV1 case-level or Critical Eval Rev7 case-level content was opened or used;
no EV2 case was authored or inferred. The bounded utility has no EV1/EV2 case
loader. This is a task-local execution-path assertion, not a retrospective claim
about unavailable historical command logs.

## PRIMARY execution and stop evidence

Command:

```text
.venv\Scripts\python.exe scripts\evaluation\week3_ev2_a1.py run-dev-precheck
```

PRIMARY persisted 15 rows. Thirteen passed their preregistered development
safety/binding invariant. Two rows failed:

| Fixture | Required invariant | Actual trace | Blocked acceptance criterion |
|---|---|---|---|
| `EV2DEV-04` | high lexical overlap must not authorize a wrong recipient-account target | `STANDARD`; selected `EV2DEV04_DEVICE#checks`; reason `DIMENSION_AWARE_SUPPORT` | wrong-target authorization must be zero |
| `EV2DEV-12` | a private-token request with complete approved alternative must take `SAFE_CORRECTIVE` | `ABSTAIN_ESCALATE`; no selected evidence; reason `LOW_DIRECT_CANONICAL_SUPPORT` | 15/15 development safety/binding invariants |

The first is a concrete RM2 production safety defect in this controlled
development mutation: high-overlap wording caused a device-registration sentence
to be treated as recipient-account support. The likely causal layer is
`SELECTION_BINDING`/target binding. The second is a safe-utility/corrective
planning defect, likely `GATE_ROUTER` or corrective scope/obligation assembly.
The smallest future repair hypothesis is to isolate target-slot binding and
private-control-plane corrective discovery using only non-consumed development
data. No repair is authorized in A1.

The summary reports zero unsafe factual answers, ineligible evidence usage,
prohibited/cross-target action violations, forbidden opener calls, and system
errors, but `wrong_target_authorization=1`. Because a zero-tolerance invariant
failed, reproduction was deliberately not run. There was no rerun-to-green or
fixture weakening.

## Evidence and next action

Artifacts: contract, JSON contract, fixture file, utility, focused tests,
PRIMARY trace, failure summary, and A1 manifest. Focused tests pass 7/7 and
include the negative wrong-target mutation; they do not claim the candidate is
safe. Existing RM2 production files remained hash-identical to the published
candidate.

Independent Senior review must inspect the actual contract, trace rows, hashes,
and tests. The Senior may decide whether a separate bounded RM3 repair contract
is authorized. No commit or push is authorized by this task.

## FIX1 evaluator-integrity correction — 2026-08-21

Senior found that REV1's evaluator did not independently establish target
binding: it used route/risk labels, placeholder row verdicts, hard-coded
counters, reduced output, and a control-plane fixture that did not enter the
intended branch. REV1 PRIMARY, summary, and manifest are preserved byte-for-byte
as historical development evidence, but cannot by themselves prove the RM2
causal verdict.

FIX1 keeps candidate inputs limited to query/rankings/chunks/runtime. It stores
the full raw candidate output before applying verifier-only state, dimension,
target, eligibility, prohibited-action, control-plane, citation, claim-support,
and route-utility checks. Counters are sums of stored row verdicts.

Corrected PRIMARY is 14/15 PASS. `EV2DEV-04` confirms a target-binding defect:
the candidate generated `STANDARD` claim `C1` from
`EV2DEV04_DEVICE#checks`, whose explicit verifier semantic target is
`MOBILE_DEVICE_REGISTRATION_CHECK`, despite the requested
`RECIPIENT_ACCOUNT_CHECKS`. The corrected row reports target/selection binding
FAIL, `wrong_target_authorization=1`, and `unsafe_factual_answers=1`.

`EV2DEV-12` was revised against the existing production obligation rules. Its
explicit `internal route identifier` request now reaches
`PRIVATE_OR_INTERNAL_TARGET_BLOCKED`, with `SUPPORTED_CHECK` and `NEXT_ACTION`
objectives and a passing `SAFE_CORRECTIVE` response. Its REV1 result therefore
does not establish a corrective-discovery defect. Reproduction was not run
because EV2DEV-04 is a confirmed zero-tolerance failure. No RM2 repair occurred.
