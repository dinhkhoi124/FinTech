# W3-003-EV2-A4-ATT1 — pre-E1 runtime environment and local model snapshot attestation

Status: `A4_ATT1_PUBLISHED_AWAITING_SENIOR_REMOTE_VERIFICATION`.

## Scope and controls

This P0 task is a read-only prerequisite for a future separately authorized E1.
It does not authorize E1, create a usable A4 receipt, read an EV2 query, or
write a consumption receipt or raw output. The frozen A3 FIX4 manifest remains
unchanged at SHA-256
`19ad35b27bd3f60e0a76ae7e42ea4c06a197166e1e9ae3ea26dbecc05ae5ee54`.

## Actual attestation result

- Runtime: `.venv-semantic\\Scripts\\python.exe`, CPython 3.11.9, 64-bit CPU.
- Dependencies: all 30 exact pins in `requirements/week1-semantic.txt` match;
  `pip check` passed.
- Offline controls: `HF_HUB_OFFLINE=1`, `TRANSFORMERS_OFFLINE=1`,
  `TOKENIZERS_PARALLELISM=false`, single-thread controls, and
  `PYTHONHASHSEED=0`.
- Model: local `sentence-transformers/all-MiniLM-L6-v2` revision
  `1110a243fdf4706b3f48f1d95db1a4f5529b4d41`; all 11 provenance files match
  path, bytes, and SHA-256; snapshot footprint is 91,578,415 bytes.
- Offline runtime load: `local_files_only=true`, zero network attempts,
  no query encode/ranking/EV2-input read, classifier parameters and corpus
  alignment verified, and frozen embedding shape `[52, 384]`.
- Runtime aggregate is
  `7cbaa6fe5ad2f18e8f4a4c7932fa20b0d50f861ba5f1a2341fcfc3cd65f025da`.
  Candidate source tree and finalized R0 decision both passed.
- Environment fingerprint:
  `f49e29f3ad3338a191f50e42e28fd2335a36f670541c9eb7337f0bcdcb478a7d`.

## Guard evidence

The deliberately non-authorizing preauth payload has SHA-256
`f7b59287f478f6fbe6878604a0d2d0ada6f943d3201aaf8cc3eabf1de4488cf3`.
`validate_a4()` rejected it with `A4_BINDING_MISMATCH:authorization` before
production runner, retrieval, EV2 query, consumption receipt, or raw-output
activity. Focused fail-closed regression tests passed 8/8.

## Regression correction

The first added missing-offline-control test failed because the helper imported
the retrieval module before checking controls. The check was moved ahead of
that import, so a missing control now fails before retrieval import or runtime
resolution. The final recorded focused suite passes 8/8.

The authoritative machine-readable evidence is in
`reports/week_03/results/w3_003_ev2_a4_*.json`. The detached review bundle is
the senior-review handoff; it contains no real authorization, EV2 output, or
consumed raw EV1/Rev7 content.

## Future E1 protocol (not executed)

The A3 A4-V3 validator does not itself bind the environment fingerprint. A
future real E1 therefore requires separately Senior-authorized just-before-
execution ATT1: reverify frozen requirements/provenance SHA/blob, set the exact
offline/thread/hash controls, use CPython 3.11.9, rerun ATT1, require environment
fingerprint `f49e29f3ad3338a191f50e42e28fd2335a36f670541c9eb7337f0bcdcb478a7d`
and every ATT1 identity, then create/use the real A4 receipt with no intervening
install, edit, or mutation. This publication does not execute that protocol.
