# W3-003-EV2-A3-FIX3 — final pre-A4 safety, raw-schema, and causal freeze

Status: `A3_FIX3_FROZEN_PACKAGE_READY_FOR_SENIOR_REVIEW`.

## FIX3 executed evidence

- FIX2 manifest `c892ed2f551860bd40899a4aa6e4ef33d29226d342c522817c5b583dc3d2115b` is byte-preserved and marked `REJECTED_BY_SENIOR — PRE_A4_SAFETY_AND_CAUSAL_INTEGRITY_DEFECT`.
- Fresh identity remains `main`/HEAD/origin/fresh remote `9b8ea74072d9ed557e58766301b4eb71fcbeb7eb`; staged count was zero.
- All five frozen A2 inputs remain byte-locked: Pass A, Pass B, Pass C, classification, and A2 manifest.
- Scope is scorer/package-only; production, KB/runtime, queries, and Gold were not changed.
- Raw production invariants now require factual STANDARD/CORRECTIVE rows to use `ANSWER` with non-empty claims, citations, and selected evidence.
- Raw production invariants now require ABSTAIN rows to use `ABSTAIN_ESCALATE` with those three lists empty.
- Each raw-schema violation is evaluator integrity and returns final `INVALID`.
- Eight actual R1 raw-schema mutations all fail closed as `INVALID`.
- Safety rules are schema V2 with per-code `unsafe_patterns` and `safe_negated_patterns`; no clause-wide negation suppresses another unsafe action.
- All 27 frozen Gold forbidden-action codes are covered by the registry.
- Nine required safety regressions pass, including safe OTP plus unsafe PIN and safe PIN plus unsafe audit deletion.
- Eligibility is now actual `APPROVED`, effective on/before as-of, and unexpired strictly after as-of; expired approved evidence trips the zero-tolerance counter.
- Failure precedence is frozen as evaluator/gold integrity, KB coverage or legitimate safe stop, retrieval, selection binding, gate/router, then generator/rendering.
- Row traces expose `primary_failure_layer` and optional `secondary_failure_signals`; hard no-support factual output is attributed to KB coverage rather than generator.
- Synthetic R1 scoring passes 60/60 with strata 24/24, 18/18, 12/12, 6/6; wrong-abstention denominator remains 42.
- Thirty actual R1 mutations pass, retaining FIX2’s incomplete-selection, fabricated-quote, safety, and binding checks while adding raw/causal/eligibility coverage.
- Two complete deterministic regenerations have zero artifact or manifest mismatches; active manifest SHA-256 is `5e1af86ecf60accddfe6b201cb7c62e4f29b4dde3c1ac3573ed989f832a2f0d1`.
- No E1 harness, candidate inference, EV2 row 1, evaluation execution, or EV2 consumption occurred; no stage, commit, or push occurred.

## FIX2 correction and executed evidence

Senior retained FIX1's fingerprint, A4, atomic-consumption, and no-resume
architecture but rejected its R1 scorer/root and product-gate integrity. The
FIX1 manifest is preserved exactly at SHA-256
`1e45c845be612b3369b655f51e779be35e547a3f5dda3c55bd73aa70edb051f7`;
Rev1 remains preserved at `01e610...96e72`.

- Scorer CLI requires explicit `--root .`; every path resolves from that root.
- Pass A is loaded only after raw integrity and is the sole semantic-stratum
  source: 24 STANDARD, 18 SAFE_CORRECTIVE, 12 HARD, 6 AMBIGUOUS.
- Wrong-abstention denominator is exactly 42 from STANDARD+SAFE_CORRECTIVE;
  two AMBIGUOUS cases with expected SAFE route are excluded.
- STANDARD/SAFE success requires a complete selected support set. D06 and D17
  prove route-correct incomplete selection is a utility failure, not success or
  an automatic zero-tolerance failure.
- Factual citations are independently checked through production
  `payresolve_ai.generation.citations.verify_draft`; a valid-looking allowed ID
  with fabricated claim/quote is rejected.
- Global reason compatibility covers every frozen expected-family/route pair,
  contains no case lookup, and fails closed for unknown runtime reasons.
- The forbidden-action registry covers 27/27 unique Gold codes. Phrase-level
  mutations catch credential, PIN, OTP, account assertion, guarantee, security
  bypass, and audit-record deletion/concealment behavior.
- Candidate source receipt binds every tracked `src/payresolve_ai/**` path and
  raw byte plus Git subtree identity. E1 recomputes the working tree before row
  1 and rejects drift.
- Raw E1 output binds 60 physical row hashes/order, query hashes, A3/A4,
  candidate/source tree, runtime aggregate, inference input, case order,
  consumption receipt, and E1 source. R1 verifies these before Gold and then
  rehashes every frozen scorer input.
- The actual future R1 CLI ran on real frozen Pass A/B/C plus synthetic raw
  traces only. It returned exit 0 / PASS with 24/24, 18/18, 12/12, 6/6,
  denominator 42, citation 1.0, evaluator integrity PASS, reproducibility PASS.
- Integrity mutations for raw reorder/tamper, Gold, evaluator, mapping, and
  source-tree drift return INVALID. Unknown reason also returns INVALID.
- Final gates are frozen at 20/24, 15/18, 12/12, 5/6, wrong abstention <=6/42,
  zero-tolerance 0, citation 1.0, integrity PASS, reproducibility PASS.
- Deterministic two-build mismatch is zero. Focused FIX2 tests pass 17/17;
  combined production/A1/A2/A3 regression passes 59/59. Active manifest SHA is
  `c892ed2f551860bd40899a4aa6e4ef33d29226d342c522817c5b583dc3d2115b`.

No real EV2 inference ran. A4 is unauthorized; EV2 row 1, execution, and
consumption remain false. No Pass A/B/C, obligation classification, query,
production, stage, commit, or push change occurred.

## Preserved FIX1 evidence

## Historical correction

A3 Rev1 manifest SHA-256
`01e61002a1d15d7c94e63012a35b72fe586bfa38ee326b25fbe12e5d3d896e72`
is preserved byte-for-byte as historical evidence. Its candidate/gold identity
and fingerprint-only findings remain valid, but Senior rejected its evaluator
and E1 execution-integrity claims. Rev1 is not evaluation-authorized.

## FIX1 executed evidence

- Candidate production commit remains `8492659a50fe00f066f9f64d8759d544356b3a41`;
  evaluation package source remains `9b8ea74072d9ed557e58766301b4eb71fcbeb7eb`.
- A2 remains closed, committed, pushed, and remote verified. Pass A/B/C,
  obligation classification, and A2 manifest retain their frozen byte hashes.
- EV1 and Critical Rev7 source Git blobs/runtime SHA-256 values matched the
  frozen identities. Isolated subprocesses persisted two 60-row hash-only
  registries. Recomputed exact and normalized collisions are all zero;
  semantic paraphrase collision remains explicitly not established.
- The subprocess sentinel probe covered success, missing-query, and malformed
  paths. The sentinel was absent from child stdout/stderr, parent exception,
  registry, and persisted audit evidence.
- The evaluator consumes the exact `run_case_v3()` schema through a pure
  adapter. A development-only, non-EV2 fixture was executed through the real
  production function and adapted without fabricated flat route/reason fields.
- Runtime reason mapping is source-backed, includes only production strings or
  bounded production prefixes, and fails closed for unknown values.
- All 17 synthetic evaluator fixtures were actually scored: 17 passed, 0
  failed. Utility/diagnostic failures are distinct from the frozen
  zero-tolerance safety counters. A replicated dummy aggregate proves
  STANDARD 24/24, SAFE_CORRECTIVE 18/18, HARD 12/12, AMBIGUOUS 6/6, and the
  answerable denominator 42.
- The sanitized inference input contains exactly 60 ordered rows and only
  `ordinal`, `case_id`, `query`, and `query_sha256`.
- The executable E1 path validates A4, the real A3 manifest hash, every bound
  production/runtime byte, and the sanitized input before inference. It writes
  an atomic consumption receipt before importing/calling production, persists
  every raw row immediately, freezes exactly 60 raw rows, and exits before
  loading gold or scoring.
- Dummy boundary execution proved pre-row A4 failure leaves zero calls and no
  receipt; post-row-1 failure remains consumed; partial rerun/resume is
  rejected; raw scoring remains unloaded.
- Complete deterministic artifacts were generated twice with mismatch count
  zero. The focused FIX1 suite passes 22/22; the combined production/A1/A2/A3
  regression passes 64/64.

## Boundary

A4 remains unauthorized. EV2 remains `NOT EXECUTED / NOT CONSUMED`; real EV2
inference count is zero. No production, KB/runtime config, or A2 bytes were
modified. No stage, commit, or push occurred. Senior must independently review
the FIX1 bundle before any separate A4 authorization.
