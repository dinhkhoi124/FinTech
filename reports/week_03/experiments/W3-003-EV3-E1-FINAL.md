# W3-003-EV3-E1-FINAL — one-shot execution boundary

Senior authorized exactly one canonical authorization, one harness process, and
one scorer process. Final preflight passed and canonical authorization 0002
validated without consumption. The single required direct script invocation
then exited 1 before the consumption boundary because Python could not import
the repository `scripts` package. No production model, inference, raw output,
Gold access, or scorer process started.

The consumption receipt, raw output, raw manifest, and score remain absent.
Because the contract forbids a second harness, retry, or alternate invocation,
this is frozen as `EV3_E1_PRECONSUMPTION_BLOCKED_NOT_CONSUMED` pending Senior
integrity adjudication. No frozen implementation, package, runtime, Gold, or
authorization byte was changed to repair the invocation environment.
