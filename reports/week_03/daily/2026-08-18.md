# Daily Report — 2026-08-18

## W3-003-EV1-C2 — portable runtime-binding correction

- Reproduced C1 `verify-package` failure in a pristine local clone at C1 with
  `core.autocrlf=false` and no ignored assets. The first fail-closed error was the
  absent classifier artifact; complete audit identified three non-encoder local
  files plus all 11 frozen MiniLM blobs.
- Preserved R, immutable C1, the 60 blind queries, 30/15/15 membership, semantic
  gold/support data, 118 atomic requirements, 61 obligations, and all metric
  thresholds.
- Split verification into Git-portable `verify-package` and local-only
  `verify-runtime-assets`. Missing assets now report
  `LOCAL_RUNTIME_ASSETS_MISSING`, not package corruption.
- Derived the execution-specific static R closure: 19 production Python modules
  and six tracked runtime inputs. Git blob SHA-256 is authoritative; worktree
  comparison normalizes CRLF and CR to LF and otherwise preserves UTF-8 bytes.
- Added a 14-asset manifest with exact 11-file snapshot-to-blob mapping, checks
  for 52 corpus chunks, `[52,384]` embeddings loaded with
  `allow_pickle=False`, and exact chunk-ID alignment.
- Built a deterministic external runtime-only asset ZIP from existing local
  assets with sorted paths, fixed timestamps, and fixed compression. It has 14
  payloads plus one internal inventory and excludes EV1 query/gold/output data.
- Hardened future authorization for `R -> C1 -> C2 -> A`, exact C1/C2 committed
  bytes, one-path A scope, and mandatory matches for both runtime ZIP SHA-256 and
  byte size plus the runtime-asset and C2 manifest hashes.
- Added receipt-first provisioning that rejects wrong ZIP SHA/bytes/inventory,
  extracts only declared payloads, and materializes all 11 snapshot paths as
  ordinary file copies without depending on symlink privileges.
- Ran the exact MiniLM load-only gate with `sentence-transformers==3.4.1`,
  `transformers==4.48.3`, `huggingface-hub==0.36.2`, and `torch==2.6.0`:
  pinned revision, CPU, dimension 384, `local_files_only=true`,
  `trust_remote_code=false`, with measured network/encode/EV1-input counters 0.
- Added CRLF/LF, semantic-mutation, missing/mutated asset, encoder inventory,
  deterministic ZIP, SHA/bytes receipt, no-network, pre-import, symlink-denial,
  and four-commit topology regression controls. The combined preserved R3 +
  Fix1 suite passed 63/63 in both the workspace and an isolated C1→synthetic-C2
  clone.
- In that clone, `verify-package` passed before assets existed; readiness first
  failed closed with `LOCAL_RUNTIME_ASSETS_MISSING`, then provisioning and the
  real offline encoder load passed. A rebuilt ZIP matched the original SHA-256
  and 83,835,711-byte size exactly.
- The Windows verification process was elevated, so no false non-admin claim is
  made. Ordinary-user portability is structural and regression-tested: the
  implementation never calls `os.symlink`, materializes copies, and passes with
  symlink creation forced to raise `PermissionError`.

Status: `W3_003_EV1_C2_FIX1_READY_FOR_SENIOR_REVIEW`.
No commit, push, authorization, retrieval, generation, encode/inference,
evaluation, or reproduction was performed. Encoder construction occurred only
in the explicitly authorized load-only readiness check.
