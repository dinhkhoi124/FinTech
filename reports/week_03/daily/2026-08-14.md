# Daily Report — 2026-08-14

## R15 F1 continuation-authority binding correction

- Reproduced `R15_CONTINUATION_AUTHORITY_NOT_PRODUCTION_BOUND`: the original migration accepted a caller-created authority dictionary.
- Production authorization now requires six exact continuation fields and exposes them only after committed-record equality, R→A topology, exact path scope, readiness hashes, environment, and runtime assets pass.
- Migration now accepts only `root`/`config_path`, invokes production verification internally, and is exposed as one-shot `migrate-r15-continuation`; it never starts Repro V0.
- Transaction order is write-once `PREPARED` receipt, atomic state replace, then atomic `PASS` receipt. Injected failure preserves legacy state and leaves recovery evidence.
- Synthetic committed R15→A15 topology and real isolated migration pass. PRIMARY 7/7 and historical runtime remain exact; future runtime absent; runtime calls 0/0/0/0.
- Active state/runtime unchanged; no reproduction retry, A15 creation, stage, commit, or push.

## R15 F2 synthetic Git-config isolation correction

- Reproduced `R15_SYNTHETIC_WORKTREE_SHARED_CONFIG_MUTATION` in a disposable repository: plain `git config` from a linked worktree changed the main repository's common `user.name`, `user.email`, and `core.autocrlf` values.
- Preflight found repository-local identity contamination (`R15 F1 Synthetic` / `r15-f1@example.invalid`) and restored only the local identity to `dinhkhoi124` / `dinhkhoi1work@gmail.com`; global and system config were not changed.
- Synthetic R15/A15 commits now use command-local author/committer environment variables and command-local `git -c core.autocrlf=false`.
- Six guards prove the real common config bytes and SHA-256 remain unchanged across worktree creation, both commits, verifier, migration, and removal.
- The local `core.autocrlf=false` baseline was not changed and is flagged `CORE_AUTOCRLF_BASELINE_REQUIRES_SENIOR_REVIEW_BEFORE_COMMIT`.
- Focused F2 regression passes with exact synthetic author/committer identity and real common-config SHA-256 unchanged at `7716d945f65b5f8723285101d384cedc194ff2ff20d6640fce8aa493dbd261d0`.
- Active state/runtime and PRIMARY evidence remain exact; reproduction was not retried; no stage, commit, or push.

## R15 F3 post-push committed-byte closure

- Reproduced `R15_POST_PUSH_COMMITTED_BYTE_CLOSURE_MISMATCH` from committed `5e89ec1`, the authorization candidate, and final F2 reviewed bytes.
- Complete audit: 62/62 paths; exactly four committed mismatches; no additional mismatch. The reviewed CLI contains `migrate-r15-continuation`, while committed `5e89ec1` does not.
- Root cause: the 62 readiness hashes came from reviewed working bytes, but proposed scope and INDEX verification covered only 41 paths; no future-tree closure gate existed.
- F3 proposes a corrective Revision-15 readiness child of `5e89ec1`, adds committed-tree and proposed-scope closure controls, and excludes unrelated user dirty files.
- A15 remains unauthorized; active state/runtime and PRIMARY remain exact; migration/reproduction/PRIMARY were not run.
