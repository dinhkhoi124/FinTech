# PayResolve AI — Operating System Note

The operating bundle has been reconciled into this repository. This file is kept
as historical context, not as a competing source of truth.

Use `AGENTS.md` for the operating contract, `docs/MASTER_PRD.md` for authoritative
scope, `PROJECT_STATE.md` for current state, `TASKS.md` for planned work, and
`docs/DEVELOPMENT.md` for commands. Markdown reports remain canonical.
